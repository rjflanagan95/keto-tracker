import React, { Component } from "react";
import "./main.css"

import Logger from "../Logger/logger";

class Main extends Component {
  constructor(props) {
    super(props)

    this.state = {
      
      }
  }

  render() {

    return (
      <Logger />
    );
  }
}

export default Main;